const theme = {
  // ${({ theme }) => theme.textColors.primary}
  textColors: {
    default: '#333',
    primary: '#CF0A0A',
  },

  // ${({ theme }) => theme.backgroundColors.primary}
  backgroundColors: {
    default: '#FFF',
    primary: '#FEF9F2',
  },
};

export default theme;
