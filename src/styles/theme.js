const theme = {
  // ${({ theme }) => theme.textColors.primary}
  textColors: {
    default: '#333',
    secondary: '#fff',
    primary: '#D81522',
  },

  // ${({ theme }) => theme.backgroundColors.primary}
  backgroundColors: {
    default: '#FFF',
    primary: '#F5F2DC',
  },
};

export default theme;
